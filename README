This F# library is intended as a generic and thread-safe alternative to System.Random, which supports operations such as "map".

This is still in very early stages, but the basic functionality is there.

Computational workflow syntax is to be supported soon.